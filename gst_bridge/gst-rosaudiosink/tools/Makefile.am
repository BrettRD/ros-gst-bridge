bin_PROGRAMS = gst-rosaudiosink

# sources used to compile this plug-in
gst_rosaudiosink_SOURCES = gstrosaudiosink.c

# compiler and linker flags used to compile the program, set in configure.ac
gst_rosaudiosink_CFLAGS = $(GST_CFLAGS)
gst_rosaudiosink_LDADD = $(GST_LIBS)

