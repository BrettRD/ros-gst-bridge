bin_PROGRAMS = gst-rosvideosink

# sources used to compile this plug-in
gst_rosvideosink_SOURCES = gstrosvideosink.c

# compiler and linker flags used to compile the program, set in configure.ac
gst_rosvideosink_CFLAGS = $(GST_CFLAGS)
gst_rosvideosink_LDADD = $(GST_LIBS)

